server {
  listen 80;
  root /usr/share/nginx/html;
  <% @digilys_instances.each_with_index do |instance, i| %>
    location /<%= instance %> {
      proxy_pass http://127.0.0.1:<%= 24500 + i %>;
      proxy_set_header Host $host;
    }
  <% end %>
}
